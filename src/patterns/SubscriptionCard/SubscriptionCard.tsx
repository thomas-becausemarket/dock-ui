import { Card, CardContent, CardHeader, CardTitle } from '~/components/ui/card';

// import '../../globals.css'
export const SubscriptionCard = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Create project</CardTitle>
      </CardHeader>
      <CardContent>
        <p>im in the content</p>
      </CardContent>
    </Card>
  );
};
